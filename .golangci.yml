# Golangci-lint configuration for Finterm project
# See CLAUDE.md §3.1 for details

version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - gocyclo
    - gocritic
    - revive
