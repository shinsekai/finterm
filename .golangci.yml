# Golangci-lint configuration for Finterm project
# See CLAUDE.md §3.1 for details

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - gocyclo
    - gocritic
    - revive

linters-settings:
  gocyclo:
    min-complexity: 15
